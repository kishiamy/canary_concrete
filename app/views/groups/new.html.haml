.row-fluid
  .content
    .page-header
      = link_to groups_path, :class => "btn" do
        %i.icon-list-alt
        = t'.list_group'
      %h1= t'.new_group'

    = render 'form'
    = link_to 'Back', groups_path, :class => "btn"
