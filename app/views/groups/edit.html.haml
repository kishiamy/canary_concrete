.row-fluid
  .content
    .page-header
      = link_to groups_path, :class => "btn" do
        %i.icon-list-alt
        = t'general.manage.list_groups'
      = link_to @group, :class => "btn btn-primary" do
        %i.icon-info-sign.icon-white
        = t'general.buttons.show'
      %h1= t'general.manage.update_group'
    = render 'form'
    
