.row-fluid
  .content
    .page-header
      = link_to pages_path, :class => 'btn' do
        %i.icon-list-alt
        = t'general.manage.list_of_pages'
      %h1= t'general.manage.new_page'

    = render :partial =>'form'
