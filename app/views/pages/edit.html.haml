.row-fluid
  .content
    .page-header
      = link_to pages_path, :class => 'btn' do
        %i.icon-list-alt
        = t'.list_pages'
      = link_to @page, :class => 'btn btn-primary' do
        %i.icon-info-sign.icon-white
        = t'.show'
      %h1= t'.edit_page'

    = render 'form'
