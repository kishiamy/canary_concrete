.row-fluid
  .content
    .page-header
      = link_to new_page_path, :class => 'btn btn-primary' do
        %i.icon-plus.icon-white
        New Page
      %h1 Listing pages
    %ul.tree
      -@pages.each do |page|
        = render :partial =>'family', :locals => { :page => page }


