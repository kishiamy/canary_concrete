.row-fluid
  .content
    .page-header
      = link_to new_page_path, :class => 'btn btn-primary' do
        %i.icon-plus.icon-white
        New Page
      %h1 Listing pages
    %table.table.table-bordered.table-striped.center
      %thead
        %tr
          %th Title
          %th Page father
          %th Actions

      %tbody
        - @pages.each do |p|
          %tr
            %td= link_to p.title, p 
            %td= p.page.title if p.page
            %td
              = link_to edit_page_path(p), :class => 'btn btn-primary' do
                %i.icon-pencil.icon-white
                Edit
              = link_to p, :class => 'btn btn-danger',:confirm => 'Are you sure?', :method => :delete do
                %i{:class =>'icon-trash icon-white'} 
                Delete


    = pages_tree
